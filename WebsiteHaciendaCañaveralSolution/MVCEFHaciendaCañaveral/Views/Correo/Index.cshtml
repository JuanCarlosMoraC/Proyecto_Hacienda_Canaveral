@model IEnumerable<MVCEFHaciendaCañaveral.Models.Correo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DatosOrganizacionales.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Email)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DatosOrganizacionales.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Email)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdCorreo }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdCorreo }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdCorreo })
        </td>
    </tr>
}

</table>
